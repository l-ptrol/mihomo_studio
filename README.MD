# Mihomo Studio (Web Editor)

[![](https://img.shields.io/badge/version-v18.2-blue)](#) [![](https://img.shields.io/badge/python-3.x-green)](#) [![](https://img.shields.io/badge/license-MIT-lightgrey)](#)

**Mihomo Studio** — легковесный веб-интерфейс для управления конфигурациями Mihomo (форк Clash) на роутерах Keenetic с Entware. Проект разработан на Python 3 и не требует "тяжелых" зависимостей, используя встроенный `http.server`.

---

## 📋 Оглавление

*   [Основные возможности](#-основные-возможности)
*   [Установка](#-установка)
*   [Использование](#-использование)
*   [Особенности реализации](#-особенности-реализации)
*   [Структура файлов](#-структура-файлов)
*   [Часто задаваемые вопросы (FAQ)](#-faq)
*   [Лицензия](#-лицензия)

---

## ✨ Основные возможности (Features)

*   **Мультипрофильность**: Создавайте разные конфигурации (например, "Дом", "Дача") и переключайтесь между ними в один клик.
*   **Редактор YAML**: Полноценный редактор с подсветкой синтаксиса и проверкой отступов.
*   **Парсер VLESS**: Вставьте ссылку `vless://...`, и сервер сам преобразует её в правильный YAML-блок.
*   **Умная вставка**: Автоматически предлагает добавить новый прокси в выбранные `proxy-groups`.
*   **Бэкапы**: Автоматическое создание резервных копий перед каждым сохранением и возможность отката (Restore) прямо из интерфейса.
*   **Управление сервисом**: Кнопка "Рестарт" в интерфейсе отправляет команду `xkeen -restart` и выводит лог запуска в консоль браузера.
*   **Темы оформления**: Dark, Light, Midnight, Cyber.

---

## 🚀 Установка (Installation)

### Требования

*   Роутер Keenetic с установленным Entware.
*   Установленный `python3`.

### Автоматическая установка

1.  Подключитесь к терминалу роутера по SSH.
2.  Выполните следующую команду:

    ```bash
    curl -sL https://raw.githubusercontent.com/your-repo/main/install.sh | bash
    ```
    *В качестве альтернативы, вы можете вручную скопировать содержимое файла [`install.sh`](./install.sh) и выполнить его в консоли.*

### Что делает скрипт установки?

Скрипт [`install.sh`](./install.sh) автоматически выполняет следующие шаги:
*   Проверяет наличие и при необходимости устанавливает `python3` и `python3-pip`.
*   Создает необходимые директории: `/opt/scripts`, `/opt/etc/mihomo/profiles`, `/opt/etc/mihomo/backup`.
*   Копирует основной скрипт [`mihomo_editor.py`](./mihomo_editor.py) и скрипт автозапуска `S95mihomo-web`.
*   Делает скрипты исполняемыми.
*   Создает профиль `default.yaml` и символическую ссылку на него, если они отсутствуют.
*   Запускает веб-сервис.

---

## 🎮 Использование (Usage)

После успешной установки веб-интерфейс будет доступен по адресу вашего роутера:
`http://<IP-адрес-роутера>:8888` (например, `http://192.168.1.1:8888`).

*   **Порт по умолчанию**: `8888`.
*   **Изменение порта**: Чтобы изменить порт, отредактируйте переменную `PORT` в файле [`mihomo_editor.py`](./mihomo_editor.py).
*   **Логирование**: Логи последнего перезапуска сервиса сохраняются в `/tmp/mihomo_last_restart.log` и также выводятся в модальном окне при нажатии кнопки "Рестарт" в интерфейсе.

---

## ⚙️ Особенности реализации (Implementation Details)

*   **Веб-сервер**: Для максимальной легковесности и минимизации зависимостей используется встроенный в Python `http.server`. Это позволяет запускать редактор на маломощных устройствах, таких как роутеры.
*   **Управление профилями**: Система профилей реализована с помощью символических ссылок (symlinks). Файл `config.yaml`, который использует Mihomo, является ссылкой на один из реальных профилей в папке `/opt/etc/mihomo/profiles/`. Такой подход обеспечивает полную совместимость со сторонними скриптами и утилитами (например, `xkeen`), которые работают напрямую с `config.yaml`.

---

## 📂 Структура файлов (File Structure)

*   `/opt/scripts/mihomo_editor.py` — Основной файл веб-сервера.
*   `/opt/etc/init.d/S95mihomo-web` — Скрипт службы для автозапуска, старта и остановки сервера.
*   `/opt/etc/mihomo/config.yaml` — Символическая ссылка на активный профиль.
*   `/opt/etc/mihomo/profiles/` — Каталог, где хранятся все ваши конфигурационные файлы (`default.yaml`, `work.yaml` и т.д.).
*   `/opt/etc/mihomo/backup/` — Каталог для автоматического сохранения резервных копий.

---

## ❓ FAQ (Часто задаваемые вопросы)

### Как изменить порт, на котором работает редактор?
Откройте файл [`mihomo_editor.py`](./mihomo_editor.py) и измените значение переменной `PORT` в самом начале файла. После этого перезапустите сервис командой `/opt/etc/init.d/S95mihomo-web restart`.

### Где хранятся логи перезапуска?
Логи последнего перезапуска можно посмотреть в файле `/tmp/mihomo_last_restart.log` на роутере. Также они отображаются в модальном окне в веб-интерфейсе после нажатия на кнопку "Рестарт".

---

## 📜 Лицензия

Проект распространяется под лицензией MIT.